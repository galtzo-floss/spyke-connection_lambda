# frozen_string_literal: true

if RUBY_ENGINE == "truffleruby" && RUBY_ENGINE_VERSION.start_with?("23.1")
  # minitest >= 5.26.2 uses endless method definitions that TruffleRuby 23.1 cannot parse.
  gem "minitest", "< 5.26.2"
end
