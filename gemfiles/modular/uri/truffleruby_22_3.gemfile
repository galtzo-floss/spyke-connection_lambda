# frozen_string_literal: true

if RUBY_ENGINE == "truffleruby" && RUBY_ENGINE_VERSION.start_with?("22.3")
  # uri >= 0.13 crashes TruffleRuby 22.3's regex engine (ShouldNotReachHere) in URI.parse,
  # which RubyGems calls while validating gemspec metadata.
  gem "uri", "< 0.13"
end
